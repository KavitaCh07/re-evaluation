import './input.css';
import React from 'react'
interface props {
    plaaceholder: string;
}

const Input = () => {
  return (
    <div>
        {/* <input placeholder={props.placeholder}/> */}
    </div>
  )
}

export default Input;